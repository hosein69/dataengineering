# گزارش کار بازبینی صفر تا صد بسته GSI — نسخه 29.9.0

**تاریخ:** 1405/07/04 (2026-09-26)  
**ورودی:** `GSI_FINAL_ONE_PACKAGE_PROCESS_CASHFLOW_COMPOSER_20260926.zip` — 1036 فایل، GSI 29.8.2 RC4-OPT2 H1  
**خروجی:** `GSI_29_9_0.zip` — GSI 29.9.0  
**نقش‌ها:** معمار نرم‌افزار · معمار بهینه‌سازی · معمار داده‌های صنعتی · معمار UI/UX · فول‌استک  
**محیط اجرا:** CPython 3.11.15 (همان حداقلِ اعلام‌شده در `requirements.txt`)، نسخه‌های کتابخانه عیناً از `requirements-validated.txt`، Chromium برای بازبینی بصری.

---

## ۰) خلاصه مدیریتی

| شاخص | بسته ورودی (29.8.2) | بسته جدید (29.9.0) |
|---|---|---|
| نتیجه `python run_all_tests.py` | **1253 موفق / 7 مجموعه ناموفق** | **1346 موفق / 0 ناموفق** |
| ممیزی مستقل frozen / extended | 173/173 · 77/77 | 173/173 · 77/77 |
| اجرای داشبورد روی Python 3.11 (حداقل اعلام‌شده) | **کرش در import** | سالم |
| موتور Cash Flow — ۱۰۰۰ پرونده | 24.45 ثانیه | **0.78 ثانیه** (خروجی بیت‌به‌بیت یکسان) |
| موتور Cash Flow — ۱۰٬۰۰۰ پرونده / ۱۴۸هزار رویداد | تخمین > ۴۰ دقیقه (رشد درجه دوم) | **11.1 ثانیه** (رشد خطی) |
| خط لوله کامل — ۲۰۰۱ ردیف نهایی (داده مصنوعی مقیاس‌شده) | 209.3 ثانیه | **151.4 ثانیه (−۲۸٪)** (۳۴/۳۴ فریم خروجی یکسان) |
| فایل‌های ریشه بسته | 219 | 24 |
| خطای صحت داده تأییدشده و رفع‌شده | — | **19** (همه با تست رگرسیون و بازتولید روی نسخه قبل) |

مهم‌ترین نتایج به زبان ساده:

1. **عددها:** در مسیر ETL، عدد اعشاری فارسی (`1٫5`) ده برابر، عدد با نماد علمی (`1e-05`) ده‌هزار برابر یا
   مبلغ‌های خیلی بزرگ (`1.5e16`) تقریباً صفر، و منفی انتهایی SAP (`1,234.56-`) مثبت خوانده می‌شد. اکنون یک
   parser واحد و سخت‌گیر برای کل سیستم وجود دارد.
2. **تاریخ‌ها:** تاریخ شمسی نامعتبر مثل `1403/13/05` بی‌صدا به یک تاریخ واقعی دیگر (`2025-03-25`) تبدیل می‌شد
   و فرمول سال کبیسه غلط بود (۱۴۰۳ را عادی می‌دانست). اکنون تاریخ نامعتبر رد می‌شود و مبدل داخلی روز‌به‌روز
   با jdatetime برای ۱۳۰۰ تا ۱۴۹۹ یکسان است.
3. **ارزها:** «دلار کانادا/هنگ‌کنگ/استرالیا» دلار آمریکا، «ریال عمان» ریال ایران و «روپیه پاکستان» روپیه هند
   شناخته می‌شد؛ «کرون سوئد» و «کرون نروژ» هر دو به کد جعلی «کرو» تبدیل و با هم جمع می‌شدند.
4. **نامعلوم ≠ صفر:** در لایه FX چند مسیر نامعلوم را صفر می‌کرد: مبلغ تخصیص نامعلوم = ۰، «بدون مهلت» =
   «۰ روز مانده»، مانده متنی نامفهوم = «تسویه‌شده».
5. **کارایی:** موتور Cash Flow و چند مرحله خط لوله درجه دوم بودند؛ روی حجم واقعی DWH عملاً قفل می‌شدند.
6. **پلتفرم:** داشبورد روی Python 3.11 اصلاً باز نمی‌شد؛ فضای مالی روی شبکه داخلی باز بود؛ اپ‌ها به اینترنت
   telemetry می‌فرستادند؛ آیکن‌ها به‌صورت کلمه انگلیسی روی برچسب‌های فارسی چاپ می‌شدند.

---

## ۱) روش کار

1. **خط مبنا:** بسته دست‌نخورده Extract شد، کتابخانه‌ها از `requirements-validated.txt` نصب شد و کل
   `run_all_tests.py`، کامپایل همه فایل‌ها و pyflakes اجرا شد.
2. **بازبینی کد به تفکیک نقش** روی هسته (`gsi/`)، اپ‌ها (`app/`)، اسکریپت‌های اجرا (`*.cmd`، `OPS/`) و خروجی‌ها.
3. **برای هر یافته صحت داده:** (الف) بازتولید روی **بسته اصلی** با ورودی مشخص، (ب) اصلاح، (ج) تست رگرسیونی که
   روی نسخه قبل شکست می‌خورد و روی نسخه جدید موفق است.
4. **برای هر تغییر کارایی:** مقایسه **بیت‌به‌بیت** همه خروجی‌ها قبل/بعد:
   * موتور Cash Flow: همه فریم‌های خروجی با `assert_frame_equal(check_exact=True)` در ۶۰، ۲۵۰ و ۱۰۰۰ پرونده؛
   * خط لوله کامل: ۳۴ فریم (df، main، to_resolve، excluded و همه extras) روی داده مصنوعی مقیاس‌شده با ورودی ثابت.
5. **بازبینی بصری:** اجرای واقعی Studio، داشبورد و فضای مالی با Snapshot منتشرشده مصنوعی و عکس‌برداری
   در عرض دسکتاپ (1440px) و موبایل (390px) + بررسی DOM در Chromium.
6. **ممیزی مستقل** (`offline_validation/frozen_audit` و `offline_validation/tests`) روی هر دو نسخه.

> داده واقعی سازمانی در این محیط در دسترس نبود. همه اندازه‌گیری‌ها روی داده مصنوعیِ هدر-واقعی
> (`tests/make_synthetic.py`) و نسخه مقیاس‌شده آن است؛ ادعای certification تولید نمی‌شود.

---

## ۲) خط مبنا — بسته ورودی چه وضعی داشت

`python run_all_tests.py` روی بسته ورودی: **1253 موفق، 7 مجموعه ناموفق.** گزارش انتشار بسته «PASS» اعلام کرده بود
چون فقط زیرمجموعه هدفمند (85 تست) و دو ممیزی مستقل را اجرا کرده بود.

| مجموعه ناموفق | علت ریشه‌ای | نوع |
|---|---|---|
| `test_process_cockpit_v28`، `test_dashboard_criticality_repair_v28`، `test_fullstack_v29_6_10` | `app/process_cockpit.py:249` یک f-string مخصوص Python 3.12 دارد (backslash داخل عبارت). روی 3.11 کل داشبورد با `SyntaxError` بالا نمی‌آید. | **باگ واقعی — بحرانی** |
| `test_design_system` | رنگ‌های hex دستی در `gsi/studio_core/streamlit_dragdrop.py` (آخرین release) به‌جای توکن طراحی | باگ واقعی — طراحی |
| `legacy_test_v26_16_hardening` | «نشت scope» کاذب: رشته `M2` در مسیر SVG نقشه فرآیند (`M2131.0 …`) پیدا می‌شد؛ داده خارج از scope نشت نکرده بود | تست شکننده |
| `legacy_test_v26_16_release` | شمارش همه `addEventListener('click'` در HTML؛ قابلیت‌های جدید listener مشروع اضافه کرده‌اند | تست کهنه |
| `legacy_test_v26_18_charts_delivery_persistence` | متن UI پس از بازطراحی عوض شده بود | تست کهنه |

تست‌های شکننده/کهنه **ضعیف نشدند** بلکه دقیق‌تر شدند: تست scope اکنون با شناسه‌های یکتا و بررسی اضافه
(`Sara` نباید در HTML باشد) می‌سنجد؛ تست lifecycle دقیقاً اتصال تب را می‌شمارد؛ تست Event Log متن صریح
حالت خالی را می‌خواهد.

pyflakes: 140 پیام، از جمله متغیرهای محاسبه‌شده ولی بی‌استفاده در `gsi/cashflow/engine.py`
(`reduced_balance`، `evidenced`) که هر پرونده یک حلقه بی‌اثر اجرا می‌کرد (حذف شد).

---

## ۳) نقش معمار داده‌های صنعتی — صحت داده

هر ردیف زیر روی **بسته اصلی** بازتولید شد و تست متناظر در `tests/test_data_accuracy_v29_9.py` است.

### D-01 تا D-05 — تبدیل عدد (شدت: بحرانی)

GSI سه parser عددی مستقل داشت (`core.text.num_safe`، `warehouse.numeric.number`،
`cashflow.engine.number`) که هرکدام بخشی از قالب‌های واقعی را درست می‌خواند. خواندن Excel با `dtype=str`
عدد اعشاری سلول را به متن `repr` تبدیل می‌کند؛ بنابراین نماد علمی در داده واقعی رخ می‌دهد.

| # | ورودی | بسته قبلی | بسته جدید | اثر |
|---|---|---|---|---|
| D-01 | `1e-05` (float) | `num_safe → 105.0` | `1e-05` | خطای ۱۰^۷ برابر |
| D-01 | `1.5e16` | `num_safe → 1.516` | `1.5e16` | مبلغ ریالی بزرگ ≈ صفر |
| D-01 | `"1.5E+06"` | `num_safe → 1.506` | `1500000` | |
| D-02 | `"1٫5"` (ممیز فارسی U+066B) | `num_safe → 15.0` | `1.5` | خطای ۱۰ برابر |
| D-02 | `"۱٬۲۳۴٫۵"` | `num_safe → 12345.0` | `1234.5` | خطای ۱۰ برابر |
| D-03 | `"1,234.56-"` (منفی انتهایی SAP) | `+1234.56` | `-1234.56` | علامت برگشته |
| D-03 | `"−1234"` (منهای یونیکد) | `+1234` | `-1234` | علامت برگشته |
| D-04 | `"\u200f1,234"`، ZWJ، BOM، LRI | ناسازگار بین سه parser | `1234` | |
| D-05 | `"1,5"` در دفتر Decimal جریان وجوه | `Decimal("15")` | رد (نامعلوم) | خطای ۱۰ برابر در دفتر مالی |

**اصلاح:** `gsi/core/numeric_parse.py` تنها پیاده‌سازی است. حالت سخت‌گیر (پیش‌فرض) فقط نمادهای بی‌ابهام را
می‌پذیرد و برای بقیه `None` برمی‌گرداند (نامعلوم، نه صفر). `num_safe` قرارداد قدیمی‌اش را حفظ کرده (خالی = 0.0 و
پذیرش `1.234.567`/`12,34`) ولی از همان هسته استفاده می‌کند؛ `warehouse.numeric` و `cashflow.engine` حالت سخت‌گیر
را صدا می‌زنند. مسیرهای مالی تصمیم‌ساز (موتور تعهد) به parser سخت‌گیر منتقل شدند (D-14).

### D-06 تا D-08 — تاریخ (شدت: بالا)

| # | ورودی | بسته قبلی (بدون jdatetime) | بسته جدید |
|---|---|---|---|
| D-06 | `1402/12/30` (۱۴۰۲ کبیسه نیست) | `2024-03-20` (= ۱۴۰۳/۰۱/۰۱) | رد |
| D-06 | `1403/07/31` | `2024-10-22` | رد |
| D-06 | `1403/13/05` | `2025-03-25` | رد |
| D-06 | `1403/00/05`، `1403/05/00` | `2024-02-22`، `2024-07-21` | رد |
| D-07 | `is_jalali_leap(1403)` / `(1404)` | `False` / `True` | `True` / `False` |
| D-08 | `2026-07-26T10:00:00` | `None` | `2026-07-26` |
| D-08 | `45371` (سلول تاریخ Excel در مسیر OOXML خام Oracle/FX/NTSW) | `None` (تاریخ گم می‌شد) | `2024-03-20` |

نکته مهم D-06: با نصب jdatetime همین ورودی‌ها `None` می‌دادند و بدون آن تاریخ اشتباه — یعنی **نتیجه به محیط
نصب بستگی داشت**. اکنون مبدل داخلی ماه/روز را اعتبارسنجی می‌کند؛ فرمول کبیسه با همان حساب ۳۳ساله مبدل
یکسان شد؛ `gregorian_to_jalali` اضافه شد. **راستی‌آزمایی:** همه روزهای ۱۳۰۰ تا ۱۴۹۹ (بیش از ۷۳ هزار روز) با
jdatetime مقایسه شد: **صفر اختلاف** در تبدیل و در کبیسه؛ رفت‌وبرگشت میلادی↔شمسی ۲۰۲۰–۲۰۳۰ بدون خطا.

### D-09 — نویسه‌های نامرئی در کلید ادغام (شدت: بالا)

`clean_key("\u200f12345")` برابر `"\u200f12345"` بود و با `"12345"` ادغام نمی‌شد (zero-match بی‌صدا). نویسه‌های
جهت‌دهی/عرض صفر/BOM/کشیده اکنون در `clean_key` و `normalize_persian_text` حذف می‌شوند؛ «ى» عربی و «ڪ» نیز
یکسان‌سازی شدند.

### D-10 — شناسایی ارز (شدت: بحرانی)

`normalize_currency` اولین alias ای را که **زیررشته** مقدار بود برنده می‌کرد و مقدار ناشناخته را به سه نویسه اول
می‌برید. بازتولید روی نسخه قبل:

| مقدار سورس | قبلی | جدید |
|---|---|---|
| دلار کانادا / دلار هنگ کنگ / دلار استرالیا / Hong Kong Dollar | USD | CAD / HKD / AUD / HKD |
| ریال عمان / ریال قطر / ریال سعودی | IRR | OMR / QAR / SAR |
| روپیه پاکستان | INR | PKR |
| USDT | USD | USDT (ناشناخته، ادغام نمی‌شود) |
| USD/EUR | USD | خالی (مبهم → قرنطینه در NTSW) |
| کرون سوئد و کرون نروژ | هر دو «کرو» (با هم جمع می‌شدند) | SEK و NOK |

**اصلاح:** طولانی‌ترین نام برنده است؛ alias لاتین فقط کلمه کامل؛ دو ارز در یک مقدار = خالی؛ ناشناخته = متن کامل
(هرگز بریده نمی‌شود). ۲۸ ارز رایج تجارت ایران به `gsi/rules/currencies.yaml` اضافه شد.

### D-11 تا D-14 — «نامعلوم» صفر می‌شد (خانواده F031، شدت: بالا)

| # | محل | بسته قبلی | بسته جدید |
|---|---|---|---|
| D-11 | `s55` دفترکل FX: مبلغ تخصیص/صف/درخواست نامعلوم (مثلاً چندارزی) | `FX_ALLOCATED_AMOUNT = 0.0` | تهی |
| D-11 | `s55` درصد رفع تعهد بدون تعهد اولیه | `0.0` | تهی |
| D-11 | `s55` مبلغ رویدادهای fx_eventlog | `0.0` | NaN |
| D-12 | `s55` پیش‌پرداخت/پروفرم/باقیمانده اعتبار | جمع روی snapshotهای تکراری یک LC + جمع EUR با CNY + نامعلوم=۰ | یک بار برای هر LC؛ چندارزی = تهی؛ ستون ارز جدید `FX_CREDIT_CURRENCY` |
| D-13 | `s56` روز تا مهلت وقتی مهلتی وجود ندارد | `0` («سررسید امروز») | NaN |
| D-13 | `s56` «تسویه‌شده» با مانده نامعلوم | ممکن (NaN→0) | فقط با مانده معلوم |
| D-14 | موتور تعهد: مانده متنی («در حال بررسی»، «?») | `0.0` و `BALANCE_IS_UNKNOWN=False` (تسویه‌شده، بدون جریمه) | نامعلوم |

### D-15 تا D-19 — سایر یافته‌های صحت

* **D-15 تاریخ در Process Explorer پول:** `pd.to_datetime` تاریخ شمسی را از دست می‌داد و `05/03/2026` را ۳ مه
  می‌خواند در حالی که دفتر مالی ۵ مارس می‌خواند؛ اکنون همان `CalendarEngine`.
* **D-16 KPI جمع مانده تعهد:** اگر حتی یک ردیف (مثلاً بارنامه‌ای که هنوز به ثبت سفارش وصل نشده) کلید نداشت،
  کل KPI «غیرقابل اتکا» می‌شد — در داده واقعی یعنی همیشه. اکنون ردیف‌های **بدون کلید و بدون مبلغ** خارج از جمع و
  صریحاً افشا می‌شوند؛ ردیفی که **مبلغ دارد** ولی کلید/ارز ندارد همچنان کل جمع را fail-closed می‌کند.
* **D-17 نرخ مرجع:** اگر ستون `value_date` وجود داشت ولی خانه خالی بود (NaN)، نرخ معتبر رد می‌شد؛ اکنون به `date` برمی‌گردد.
* **D-18 `is_empty_val(pd.NA)`** خطای `TypeError` می‌داد؛ stageهای tolerant با ستون nullable بی‌صدا به حالت قبل برمی‌گشتند.
* **D-19 پل ORDER→REG:** شماره پرونده placeholder (`0`) کلید واقعی تلقی می‌شد و join مرکز پرونده ضرب دکارتی
  می‌ساخت؛ بازتولید روی نسخه قبل: سفارش‌های `500001` و `500002` بی‌ربط به REG `11111111` نسبت داده شدند.

---

## ۴) نقش معمار بهینه‌سازی

### P-01 موتور Cash Flow (شدت: بحرانی برای مقیاس)

حلقه‌های درجه دوم شناسایی‌شده در `gsi/cashflow/engine.py`:
timeline (پرونده × ۱۲ مرحله × کل رویدادها)، زنجیره تعهد (پرونده × رویدادها)، خلاصه‌ها (گروه × لینک‌ها و گروه ×
رویدادهای ابطال)، مانده حساب‌ها (حساب × حرکت‌ها)، ظرفیت لینک (لینک × لینک)، تطبیق Snapshot (سنجش × سنجش)
و از همه بدتر `select_rate` که **برای هر رویداد نقدی کل DataFrame نرخ را به dict تبدیل و دوباره parse می‌کرد.**

**اصلاح:** index یک‌باره (`PreparedRates`، گروه‌بندی رویداد/لینک/سنجش) با **همان ترتیب پیمایش** تا جمع‌های
Decimal بیت‌به‌بیت یکسان بمانند؛ cache انتخاب نرخ.

| پرونده | رویداد | قبل | بعد | تسریع |
|---|---|---|---|---|
| 60 | 891 | 0.69 s | 0.06 s | ×11 |
| 250 | 3,705 | 3.19 s | 0.19 s | ×17 |
| 1,000 | 14,808 | 24.45 s | 0.78 s | ×31 |
| 3,000 | 44,424 | (≈ ۴ دقیقه) | 2.93 s | |
| 10,000 | 148,078 | (> ۴۰ دقیقه) | 11.10 s | |

همه فریم‌های خروجی در ۶۰/۲۵۰/۱۰۰۰ پرونده **IDENTICAL** (مقایسه دقیق). تست `test_cashflow_performance_v29_9`.

### P-02 تا P-09 — خط لوله اصلی

پروفایل با cProfile روی ۲۰۰ و ۴۰۰ برابر داده مصنوعی (۱۰۰۱ و ۲۰۰۱ ردیف نهایی) برای جداکردن هزینه خطی از
فوق‌خطی:

| # | مشکل | اصلاح |
|---|---|---|
| P-02 | pandas ≥ 2.1 روی **هر** عملیات، `DataFrame.attrs` را deepcopy می‌کند؛ adapterها لیست کامل هدرهای سورس را در attrs نگه می‌داشتند: ۱۰ میلیون فراخوانی deepcopy، ≈۸٪ زمان | `SharedList` (لیست فقط‌خواندنی که کپی نمی‌شود؛ JSON یکسان) |
| P-03 | `s57`: برای هر ردیف دفترکل، کل فریم‌های پل نرخ/جابه‌جایی/مغایرت دوباره فیلتر می‌شد — O(n²) | index یک‌باره بر اساس REG |
| P-04 | `groupby` روی فریم با صدها ستون در `s40` (بحرانی بودن BL/سفارش)، موجودی کارشناسی و `s57`، در حالی که ۴–۱۰ ستون خوانده می‌شد | projection به ستون‌های مصرفی |
| P-05 | `_partial_total` برای هر کلید سفارش×متریال پنج DataFrame موقت می‌ساخت | حلقه ساده با همان ترتیب جمع pandas |
| P-06 | `s58`: هر فراخوانی `stage()` یک فیلتر boolean (≈۱۰ بار برای هر REG) | dict اولین ردیف هر مرحله |
| P-07 | `is_empty_val` (۵٫۸ میلیون فراخوانی) در هر فراخوانی یک set تازه می‌ساخت | frozenset + مسیر سریع رشته |
| P-08 | `encode` انبار داده (۹٫۵ میلیون فراخوانی) برای هر سلول import و زنجیره isinstance | مسیر سریع نوع‌های ساده |
| P-09 | `std()` در adapterها `find_col` را برای هر نگاشت دو بار اجرا می‌کرد | یک بار |

**نتیجه:** خط لوله کامل روی ۲۰۰۱ ردیف: 209.3 ثانیه (بسته اصلی) / 214.1 ثانیه (پس از اصلاحات صحت، پیش از بهینه‌سازی) → **151.4 ثانیه (−۲۸٪)**؛ همه ۳۴ فریم خروجی قبل و بعد
**IDENTICAL**. اثر روی حجم واقعی بیشتر است چون بخش‌های حذف‌شده فوق‌خطی یا متناسب با پهنای جدول بودند.
باقی زمان عمدتاً خواندن Excel/openpyxl و ذخیره شواهد فیزیکی در SQLite است (قرارداد ممیزی؛ دست نخورد).

---

## ۵) نقش فول‌استک

| # | یافته | شدت | اصلاح |
|---|---|---|---|
| F-01 | داشبورد روی Python 3.11 (حداقل اعلام‌شده در `requirements.txt` و نصب‌کننده) با `SyntaxError` بالا نمی‌آمد | بحرانی | اصلاح کد + تست کامپایل همه فایل‌ها |
| F-02 | `RUN_FINANCIAL_WORKSPACE.cmd`: Python سراسری به‌جای `.venv`، بدون `cd` (config خوانده نمی‌شد) و بدون محدودیت آدرس — **فضای مالی روی شبکه داخلی باز بود** | بالا (امنیت) | بازنویسی مثل `START_GSI.cmd` + `address=127.0.0.1` در config |
| F-03 | اپ‌ها telemetry Streamlit را به اینترنت می‌فرستادند (در کنسول مرورگر: `ERR_TUNNEL_CONNECTION_FAILED`) | متوسط (حریم خصوصی/آفلاین) | `gatherUsageStats=false` |
| F-04 | دکمه Deploy و منوی توسعه‌دهنده برای کاربر نهایی | پایین | `toolbarMode=viewer` |
| F-05 | همه `.cmd`ها LF-only بودند (کنترل‌پنل اصلی ۳۵ `goto`/`call :label` دارد؛ cmd.exe روی فایل LF-only در یافتن label ناپایدار است) و یک فایل نویسه غیرASCII داشت | متوسط | ASCII + CRLF، `.gitattributes`، تست |
| F-06 | پیش‌فرض `D:\GSI_DATA` روی سیستم غیرWindows پوشه‌ای **نسبی** به همین نام داخل بسته می‌ساخت (ریشه حادثه «داده runtime داخل ZIP» در گزارش قبلی) | متوسط | `default_data_root()` وابسته به سیستم‌عامل + جداسازی `GSI_DATA_ROOT` در test runner |
| F-07 | `app/` پکیج namespace بود و UI-kit همراه یک `app.py` دارد؛ پس از رندر Process Explorer پول، `import app.theme` در همان پروسه شکست می‌خورد. همچنین insert/remove مسیر در `sys.path` بین نشست‌های هم‌زمان Streamlit race داشت | متوسط | `app/__init__.py`، افزودن idempotent مسیر (append) |
| F-08 | وابستگی‌ها بدون سقف: نصب امروز pandas 3 و Streamlit 2 را می‌آورد که پیش‌فرض‌های مورد اتکای کد را عوض می‌کنند | متوسط | سقف major تست‌شده |
| F-09 | مسیر مطلق فایل روی سرور (نام کاربر/پوشه) در نوار کناری Studio | پایین (افشای اطلاعات) | حذف |

---

## ۶) نقش معمار UI/UX

بررسی با اجرای واقعی سه اپ و Chromium (عکس‌ها در دسکتاپ و موبایل، بدون اسکرول افقی در ۳۹۰px):

| # | یافته | اصلاح |
|---|---|---|
| U-01 | قاعده سراسری فونت (`* { font-family: … !important }`) روی آیکن‌های Material هم اعمال می‌شد: به‌جای فلش، کلمه `keyboard_arrow_right` روی عنوان expander و دکمه سایدبار چاپ می‌شد (در DOM اندازه‌گیری شد) | استثنای `[data-testid="stIconMaterial"]` |
| U-02 | رنگ primary پیش‌فرض Streamlit (قرمز) روی chipهای «ایمن» و دکمه‌های اصلی — خلاف معنای رنگ در سیستم طراحی | تم از `gsi/design/tokens.py` در config؛ تست هم‌خوانی |
| U-03 | کارت KPI «جمع مانده تعهد» جمله هشدار را با اندازه عدد چاپ می‌کرد؛ مبالغ چندارزی در یک خط به‌هم می‌ریخت | پیام → «—» + زیرنویس؛ هر ارز یک خط با ایزوله جهت |
| U-04 | تاریخ ISO در متن راست‌به‌چپ جابه‌جا نمایش داده می‌شد (`2026-08-31` → «31-08-2026») و در هیچ‌جای محصول تاریخ شمسی نبود | `date_label`: «1405/06/09 (2026-08-31)» با ایزوله یونیکد، بدون شکستن خط؛ در HTML، Studio، داشبورد و فضای مالی |
| U-05 | تاریخ مرجع پیش‌فرض «امروز» بود: فردای هر Refresh، آخرین Snapshot سالم با برچسب «کهنه» نمایش داده می‌شد | پیش‌فرض = تاریخ Snapshot منتشرشده |
| U-06 | ورودی تاریخ فقط `YYYY-MM-DD` | شمسی یا میلادی |
| U-07 | صفحه فضای مالی چپ‌به‌راست، با تم پیش‌فرض، و تاریخ گزارش = امروز (پس همه تطبیق مانده‌ها «عدم تطابق تاریخ») | RTL + تم GSI + تاریخ Snapshot + هشدار در تاریخ متفاوت |
| U-08 | hex دستی در کامپوننت drag-drop | توکن |

---

## ۷) نقش معمار نرم‌افزار

| # | یافته | اصلاح |
|---|---|---|
| A-01 | ۲۱۹ فایل در ریشه (۱۹۴ یادداشت انتشار، gate، لاگ و patch نسخه‌های قبل) — پیدا کردن ورودی اجرا برای کاربر دشوار بود | ریشه ۲۴ فایل؛ همه اسناد قبلی **بدون تغییر** در `docs/history/`؛ `docs/ARCHITECTURE_FA.md` جدید؛ Runbook در `docs/RUNBOOK_DWH_FA.md` |
| A-02 | سه parser عددی واگرا | یک ماژول (D-01) |
| A-03 | نسخه و نام manifest در سازنده بسته هاردکد بود؛ `.venv` مستثنا نبود | نسخه از `gsi/factsheet.py`؛ manifest `PACKAGE_SHA256.json` |
| A-04 | اسکریپت ساخت نمونه‌های HTML عمداً از بسته حذف شده بود؛ نمونه‌ها بعد از تغییر کد قابل بازتولید نبودند | `tools/make_release_samples.py` از مسیر واقعی خط لوله |
| A-05 | Gate انتشار «PASS» بر اساس زیرمجموعه تست | runner کامل + دو ممیزی مستقل در این گزارش |
| A-06 | کد مرده در موتور Cash Flow | حذف |
| A-07 | ورودی‌های نسخه‌دار (`VERIFY_RUNTIME_V29_6_4.cmd`، `RUN_STUDIO_V28.cmd`) | `VERIFY_RUNTIME.cmd`؛ `RUN_STUDIO_V28.cmd` = alias سازگاری |

**تصمیم‌های آگاهانه (عدم تغییر):**
* authority سورس‌ها، RuleBook، فرمول‌های تعهد/جریمه و grainها تغییر نکرد.
* قرارداد قدیمی `num_safe` (خالی = 0.0) برای سازگاری حفظ شد؛ سطوح تصمیم‌ساز به parser سخت‌گیر منتقل شدند.
* خروجی `rates` در adapter خرید ارز (میانه نرخ شامل «در برنامه خرید») هیچ مصرف‌کننده‌ای ندارد؛ حذف نشد، ثبت شد.
* تفسیر `05/03/2026` به‌صورت روز/ماه/سال (قرارداد موجود) حفظ شد؛ فرمت ماه/روز آمریکایی قابل تشخیص خودکار نیست.

---

## ۸) اعتبارسنجی نهایی

| بررسی | نتیجه |
|---|---|
| `python run_all_tests.py` (CPython 3.11.15) | **1346 موفق / 0 ناموفق** |
| ممیزی مستقل frozen (`offline_validation/frozen_audit`) | 173/173 |
| ممیزی گسترده (`offline_validation/tests`) | 77/77 |
| کامپایل همه فایل‌های Python روی 3.11 | PASS (قفل با تست) |
| هم‌ارزی موتور Cash Flow (۶۰/۲۵۰/۱۰۰۰ پرونده) | همه فریم‌ها یکسان |
| هم‌ارزی خط لوله (۳۴ فریم، داده مقیاس‌شده، ورودی ثابت) | IDENTICAL |
| مبدل شمسی در برابر jdatetime (۱۳۰۰–۱۴۹۹) | ۰ اختلاف |
| اجرای واقعی Studio/داشبورد/فضای مالی | بدون خطای کنسول؛ بدون اسکرول افقی در ۳۹۰px |

تست‌های جدید: `tests/test_data_accuracy_v29_9.py` (۴۴)، `tests/test_cashflow_performance_v29_9.py` (۳)،
`tests/test_platform_hardening_v29_9.py` (۱۰).

---

## ۹) فهرست فایل‌های تغییریافته

**هسته (`gsi/`):** `core/numeric_parse.py` (جدید)، `core/text.py`، `core/jalali.py`، `warehouse/numeric.py`،
`warehouse/store.py`، `warehouse/service.py`، `cashflow/engine.py`، `cashflow/process_design.py`، `cashflow/ui.py`،
`cashflow/report.py`، `engines/commitment.py`، `rulebook/loader.py`، `rules/currencies.yaml`،
`stages/s40_criticality.py`، `stages/s55_fx_traceability.py`، `stages/s56_money_flow_control.py`،
`stages/s57_legacy_knowledge.py`، `stages/s58_case_actions.py`، `adapters/base.py`، `adapters/moghavemat.py`,
`resolve/registration_bridge.py`، `report/financial_summary.py`، `studio_core/html_export.py`،
`studio_core/streamlit_dragdrop.py`، `process_intelligence/core.py`، `integrations/daily_email.py` (توضیح)، `factsheet.py` (نسخه).

**اپ‌ها (`app/`):** `__init__.py` (جدید)، `styles.py`، `studio.py`، `dashboard.py`، `cashflow.py`، `process_cockpit.py`.

**اجرا و عملیات:** `.streamlit/config.toml` (جدید)، `.gitattributes` (جدید)، `.gitignore`، همه `*.cmd` (CRLF/ASCII)،
`RUN_FINANCIAL_WORKSPACE.cmd` (بازنویسی)، `RUN_STUDIO_V28.cmd` (alias)، `VERIFY_RUNTIME.cmd` (تغییر نام)،
`OPS/GSI_DWH_CONTROL.cmd`، `OPS/GSI_ENV.cmd`، `OPS/gsi_dwh_ops.py`، `requirements.txt`، `run_all_tests.py`.

**ابزار:** `tools/build_clean_release.py`، `tools/cashflow_synthetic.py` (جدید)، `tools/make_release_samples.py` (جدید).

**تست:** سه فایل جدید بالا + اصلاح دقیق سه تست کهنه/شکننده + ارجاع مسیر سند در یک تست.

**اسناد:** `README.md`، `INSTALL.md`، `CHANGELOG.md`، `KNOWN_LIMITATIONS.md`، `PACKAGE_INFO.json`،
`00_READ_ME_FIRST_FA.txt`، `docs/ARCHITECTURE_FA.md` (جدید)، همین گزارش؛ ۱۹۴ سند قبلی به `docs/history/`.

**نمونه‌ها:** `samples/release_html/*` با کد جدید دوباره ساخته شدند (داده ساختگی).

---

## ۱۰) محدودیت‌های باز و پیشنهاد گام بعد

1. **اجرای روی داده واقعی سازمان (ضروری قبل از تولید):** `REFRESH_GSI_DATA.cmd` → `VERIFY` روی Snapshot واقعی؛
   مقایسه چند عدد کلیدی (جمع مانده تعهد به تفکیک ارز، تعداد REG، تعداد قرنطینه ارز «مبهم») با نسخه قبل. انتظار
   می‌رود برخی ردیف‌ها با ارز قبلاً اشتباه (مثلاً دلار کانادا) اکنون در ارز درست بنشینند و چند مقدار قبلاً «۰» اکنون «—» باشند.
2. **ارزهای ناشناخته:** پس از اولین Refresh، مقادیر ارزی که کد ISO نگرفتند را از قرنطینه/لاگ بخوانید و به
   `gsi/rules/currencies.yaml` اضافه کنید.
3. **قواعد موقت منقضی (2026-09-22):** RuleBook تا ثبت تصمیم رسمی fail-closed می‌ماند (`python -m gsi doctor`).
4. **چندکاربره:** اپ‌ها برای یک اپراتور روی localhost طراحی شده‌اند (تاریخ مرجع در محیط پروسه نوشته می‌شود).
   برای سرور مشترک، انتقال `GSI_TODAY` به state نشست پیشنهاد می‌شود.
5. **کارایی بیشتر:** بزرگ‌ترین هزینه باقی‌مانده خواندن Excel و ذخیره سلول‌های فیزیکی است؛ گام بعد، cache
   فریم استانداردشده بر اساس هش فایل سورس (بدون تغییر قرارداد ممیزی).
6. **Windows/Outlook/SMB acceptance** همچنان باید روی ماشین سازمان انجام شود.

---

## ۱۱) استقرار نسخه جدید

1. `GSI_29_9_0.zip` را در پوشه **جدید** محلی Extract کنید (مثلاً `D:\GSI_APP_29_9\`).
2. `00_RUN_GSI.cmd` → نصب runtime (`.venv` تازه) → **Doctor → Refresh → Verify → Start GSI**.
3. انبار داده `D:\GSI_DATA` سازگار است و مهاجرت لازم ندارد؛ اصلاحات صحت داده از اولین Refresh اعمال می‌شوند.
4. برای اطمینان از کد در حال اجرا: `VERIFY_RUNTIME.cmd` باید `GSI_VERSION= 29.9.0` نشان دهد.
