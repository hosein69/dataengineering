# V29.6.5 — همگام‌سازی واقعی دید تأمین متریال با HTML

- خروجی HTML اکنون همان `build_material_view()` را که در تب Streamlit «دید تأمین — متریال محور» دیده می‌شود دریافت می‌کند.
- این View به صورت یک تب مستقل و فقط یک‌بار در HTML ظاهر می‌شود؛ نه جدول شناور بالای همه تب‌ها.
- تب متریال dataset مستقل دارد و فیلترهای خودش را روی متریال، موقعیت فعلی، حوزه معطل، مالک قطعه و مرحله فعلی اعمال می‌کند.
- Export Excel داخل همان تب از برش فیلترشده همان View ساخته می‌شود.
- Grain تب‌های Composer و KPIهای اصلی تغییر نکرده‌اند؛ View متریال وارد محاسبات پایه نمی‌شود.
- Build قابل مشاهده: 29.6.5.
