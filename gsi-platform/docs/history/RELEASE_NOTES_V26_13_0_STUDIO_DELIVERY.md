# AIBL 26.13.0 — Studio Delivery, Dynamic HTML, Outlook, Process Explorer

- Dynamic HTML charts synchronized with active filters.
- Process Explorer section in HTML using filtered Event Log/Bottlenecks.
- Filtered Studio Excel export remains the source snapshot for custom tabs.
- Classic Outlook display/send for the current filtered Studio report.
- Selected email charts and Excel attachment are sent from the same filtered snapshot.
- Manifest regenerated; doctor passes.

## Outlook
Classic Outlook on Windows is required for direct COM send/display. Install `pywin32` optionally.
