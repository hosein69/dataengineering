# TARGET_ARCHITECTURE — constrained target, not an implemented claim

Keep the modular Python/pandas/SQLite/Streamlit stack. Raw source bytes are immutable; standardized, derived, snapshot and event representations are separate. Publish a complete validated run and pin readers to it. Observe downstream evidence without creating upstream completion.

Implemented increment: typed run snapshots, run-addressed files after gates, row preservation fixes, native PO semantics, limited finance/status/KB corrections and small warehouse UX changes.

Next prerequisites: explicit full/delta source contracts; native event identifiers; stable object-centric case IDs; precise per-field source authority; quarantine facts for orphan finance; mandatory raw-header contracts; published legacy obligation totals; unknown/zero parity; complete evidence-bound process chatbot; operational deployment ACL. These are not presented as already delivered.
