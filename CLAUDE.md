# راهنمای مخزن

## نظام طراحی

**هر کار بصری در این مخزن از «نظام طراحی البرز» پیروی می‌کند.**

وقتی کاربر گفت «البرز»، «نظام طراحی»، «همان طراحی»، یا خواست پوستر /
گزارش / رابط بسازی یا تغییر دهی — **اول `DESIGN_SYSTEM_ALBORZ.md` را
بخوان**، بعد دست به کار شو.

* سند: `DESIGN_SYSTEM_ALBORZ.md` (نسخهٔ یکسان در ریشه و در هر دو پکیج)
* کد: `aibl-supply-chain/aibl/report/alborz.py` و
  `hr-performance/hrperf/report/alborz.py` — **منبع واحد رنگ**
* فیگما: https://www.figma.com/design/XOPK0BH9oZpkSID19awXJe

هیچ رنگی را جای دیگری تعریف نکن. اگر رنگ تازه لازم شد، به `alborz.py`
اضافه کن و در **هر دو** پکیج.

## دو پکیج

| پکیج | چیست | تست‌ها |
|---|---|---|
| `aibl-supply-chain` | زنجیرهٔ تأمین خرید خارجی | `python run_all_tests.py` |
| `hr-performance` | عملکرد منابع انسانی | `python run_all_tests.py` |

هر دو پکیج **عمداً مستقل**اند: `alborz.py`، `aqua.py` و `dispatch.py`
در هر دو کپی شده‌اند تا جداگانه نصب و اجرا شوند. هر تغییر باید در هر دو
اعمال شود؛ تست معماری این را می‌سنجد.

## قاعده‌های همیشگی

* **کنتراست ادعا نیست، اندازه‌گیری است.** هر متن ≥۴٫۵:۱ (۳:۱ برای متن
  بزرگ). `tests/test_packaging.py` این را روی بسته می‌سنجد، نه روی درخت کد.
* **بسته‌بندی فقط با `make_package.py`.** دستور پوستهٔ دستی، فایل‌های
  نقطه‌دار (`.streamlit/config.toml`) را جا می‌گذارد و رابط را از کار
  می‌اندازد. این یک بار در تولید اتفاق افتاد.
* **برند سازمانی**، نه نام پکیج: IKCO · Global Sourcing (GS) ·
  Governance and Integration (GI) · Data Analytics and KPI.
  «AIBL» فقط نام داخلی پکیج پایتون است.
* **پیش از تحویل هر تغییر بصری، در مرورگر واقعی ببین.** تست سبز، دلیل
  درست‌بودن ظاهر نیست.

## دو سیستم‌عامل

محیط کار روی **مک** است؛ تولید هنوز ویندوز هم دارد. سه چیز فرق می‌کند و
هر سه در `tests/test_packaging.py::test_cross_platform` قفل است:

* **ارسال.** «ارسال با یک کلیک» از COM اتلوکِ ویندوز می‌آید و مک آن را
  ندارد. `dispatch.backend()` بستر را می‌گوید و `dispatch.action_label()`
  برچسب دکمه را. رابط هرگز «ارسال» را هاردکد نمی‌کند: روی مک پیام فقط
  **باز** می‌شود و `send_now=True` صریحاً خطا می‌دهد، نه تنزلِ بی‌صدا.
* **مسیر داده.** مسیر UNC روی مک وجود ندارد. `settings._net()` مسیرها را
  با جداکنندهٔ همین سیستم می‌سازد؛ ریشه با `AIBL_NET` عوض می‌شود
  (پیش‌فرض مک: `/Volumes/data-share/Global Sourcing`).
* **راه‌انداز و فونت.** `run_mac.command` — با بیت اجرا، که بسته‌بندی
  بازرسی‌اش می‌کند. زنجیرهٔ فونت جانشین مک دارد (`SF Arabic`,
  `Geeza Pro`) و `local()`ها نام‌های PostScript را هم می‌گیرند.

راهنمای کامل: `INSTALL_MAC.md` در هر دو پکیج.
