# وضعیت پروژه — GSI Data Trust

> **این فایل نقطه ورود هر مدل/همکار جدید است.** قبل از هر کاری این را کامل بخوانید.
> هر کسی که روی این پروژه کار می‌کند، موظف است پیش از پایان کارش این فایل را به‌روز کند.

آخرین به‌روزرسانی: 2026-09-26 (فاز B تمام) · شاخه کاری: `claude/package-architecture-optimization-158sqw`

---

## ۱) این پروژه چیست

سازمان (خرید خارجی / زنجیره تأمین IKCO) یک سامانه کامل دارد — **GSI 29.9.0** در `gsi-platform/` —
ولی **داده ورودی‌اش کیفیت پایینی دارد و در «نقطه صفر» است**. هدف این فاز، کد زدن روی قابلیت جدید
نیست؛ هدف این است که:

1. **داده غلط مبنای تصمیم نشود** — نه با حذف، بلکه با درجه‌بندی «قابلیت تصمیم‌گیری» هر عدد.
2. **داده کمی کثیف حذف نشود** — در زنجیره تأمین، ردیف‌های ناقص معمولاً همان پرونده‌های مشکل‌دارند
   (پرونده گیرکرده در گمرک تاریخ ندارد چون کسی به‌روزش نکرده). حذفشان یعنی حذف دقیقاً همان
   چیزی که مدیر باید ببیند.
3. **فرهنگ داده با UI/UX بالا برود** — نه با شعار، بلکه با نشان‌دادن مالک، کوچک‌ترین اقدام بعدی،
   و ارزشی که آن اقدام آزاد می‌کند.

استراتژی کامل: **`gsi-platform/docs/DATA_STRATEGY_FA.md`** (اول آن را بخوانید).

## ۲) وضعیت فعلی — چه چیزی تمام شده

| فاز | وضعیت | خروجی |
|---|---|---|
| بازبینی و دیباگ بسته ورودی (29.8.2 → 29.9.0) | ✅ تمام | `gsi-platform/`، گزارش `GSI_REVIEW_REPORT_V29_9_0_FA.md`، ۱۳۴۶ تست سبز |
| استراتژی داده (نقطه صفر → بلوغ) | ✅ تمام | `gsi-platform/docs/DATA_STRATEGY_FA.md` |
| لایه اعتماد داده — هسته (فاز A) | ✅ تمام | `gsi-platform/gsi/trust/` — ۲۱ تست |
| اتصال به خط لوله + ماندگاری + روند (فاز B) | ✅ تمام | `gsi/stages/s95_trust.py`، `gsi/trust/trend.py` |
| سطح UI فرهنگ داده (فاز C) | ✅ تمام | `gsi-platform/app/trust_view.py` — ۵ تب |
| بسته‌بندی و گزارش (فاز D) | ✅ تمام | `GSI_29_10_0.zip` + `GSI_TRUST_LAYER_REPORT_V29_10_0_FA.md` |

جزئیات ریز هر فاز: بخش ۵.

## ۳) قواعد غیرقابل‌مذاکره این پروژه

هر کسی این کد را ادامه می‌دهد، این‌ها را **نقض نکند** (هر کدام تست دارد):

1. **حذف ممنوع.** هیچ ردیف/سلولی به‌خاطر کیفیت پایین حذف یا فیلتر نمی‌شود. فقط *علامت* می‌خورد.
2. **نامعلوم ≠ صفر.** مقدار نامعلوم `None`/`NaN`/`—` است، هرگز `0`.
3. **جمع بین ارزهای مختلف ممنوع.**
4. **هیچ عددی بدون درجه اعتماد نمایش داده نمی‌شود** در سطوح تصمیم‌ساز.
5. **هر ایراد باید شاهد داشته باشد** — فایل/شیت/ردیف/ستون/مقدار خام. بدون شاهد، ایراد ثبت نمی‌شود.
6. **مالک هرگز سرزنش نمی‌شود.** معیار، *نرخ بهبود* است نه مقدار مطلق (مقدار مطلق اول کار بد است
   و تقصیر کسی نیست؛ سرزنش = مرگ فرهنگ داده).
7. **قرارداد سورس و authority تغییر نمی‌کند** بدون تأیید صریح مالک کسب‌وکار.
8. **آفلاین.** هیچ فراخوان اینترنتی اضافه نمی‌شود.

9. **Continuity-first.** قبل از شروع قابلیت جدید، `PROJECT_STATE.md` و
   `HANDOFF_FOR_NEXT_MODEL.md` باید نقطه ادامه، تصمیم‌های معماری و کار نیمه‌تمام را دقیق
   نشان دهند؛ هیچ فاز مهمی فقط در حافظه مدل/چت باقی نمی‌ماند.
10. **مدل مالکیت را تخت نکنید.** `EXPERT_*` مالک تخصصی هر حوزه است و
    `ORG_DEPT / ORG_UNIT / ORG_MANAGER` زمینه سازمانی و escalation را می‌دهند.
    owner عمومی فقط fallback است. این ساختار، اهرم اصلی UI فرهنگ داده است.

## ۴) چطور کار کنم (دستورهای عملی)

```bash
cd gsi-platform

# محیط (یک‌بار)
python3 -m venv .venv && .venv/bin/pip install -r requirements-validated.txt

# همه تست‌ها — باید «0 ناموفق» بدهد
.venv/bin/python run_all_tests.py

# فقط تست‌های لایه اعتماد
PYTHONPATH=. .venv/bin/python -m pytest -q tests/test_trust_*.py

# ساخت بسته تمیز + مانیفست هش
.venv/bin/python tools/build_clean_release.py ../gsi-release/GSI_<version>.zip
```

**قبل از هر commit:** `run_all_tests.py` باید صفر ناموفق بدهد.

## ۵) نقشه راه فنی — وضعیت ریز

### فاز A — هسته لایه اعتماد (`gsi/trust/`) ✅
- [x] `codes.py` — تاکسونومی کد ایراد (شناسه پایدار، بُعد کیفیت، نوع اقدام، متن فارسی)
- [x] `verdict.py` — نمونه ایراد + شاهد فیزیکی + مالک؛ دفتر append-only
- [x] `profiling.py` — پروفایلر «نقطه صفر»: کارنامه فیلد و دفتر ایرادها
- [x] `fitness.py` — قرارداد تصمیم و درجه‌بندی (قابل تصمیم / جهت‌نما / غیرقابل استناد)
- [x] `impact.py` — «کوچک‌ترین اقدام بعدی» + کارنامه مالک
- [x] `contracts.py` — پنج تصمیم واقعی GSI با مسیریابی به نقش مالک هر فیلد
- [x] `tests/test_trust_layer_v29_10.py` — ۲۸ تست قفل‌کننده

### فاز B — اتصال ✅
- [x] `gsi/stages/s95_trust.py` — اجرا روی مارت نهایی، بدون تغییر هیچ مقدار کسب‌وکاری
- [x] سه ستون سبک روی هر ردیف برای فیلتر (`TRUST_STATE` / `TRUST_BLOCKERS` / `TRUST_NOT_READY_FOR`)
- [x] `bridge.trust_snapshot` — ثبت عکس کیفیت هر اجرا
- [x] `gsi/trust/trend.py` — روند بهبود و دلتا بین اجراها

### فاز C — UI فرهنگ داده ✅

> **قرارداد Phase C:** Phase C باید ownership را در تمام سطوح `EXPERT_*` + `ORG_DEPT/UNIT/MANAGER`
> حفظ کند؛ کاربر باید بتواند از تصمیم/ایراد به نقش، فرد، واحد سازمانی و شاهد سورس برسد.
- [x] صفحه «اعتماد داده» با درجه هر تصمیم و drill-down تا سلول سورس
      (`app/trust_view.py`، محیط کاری «اعتماد داده و کیفیت» در Studio)
- [x] کارنامه مالک + فهرست کار هر نقش (خروجی Excel)
- [x] **سه سطح slice**: فرد / اداره / مدیر — `impact.ORG_LEVELS`
- [x] مسیر escalation روی هر ایراد: `ORG_DEPT` + `ORG_MANAGER` (fallback `ORG_HEAD`) + `ORG_VICE`
- [x] نمایش روند بهبود (کارت دلتا + نمودار «ایراد باز»)

**دو باگ که در همین فاز پیدا و رفع شد** (جزئیات در `HANDOFF_FOR_NEXT_MODEL.md` بخش ۳):
1. `FIELD_NEVER_POPULATED` از دفتر حذف می‌شد ولی فهرست کار از روی *وضعیت فیلدها*
   بازسازی می‌شد، پس همان نگاشت‌نبودن دوباره به شکل «۵ سلول را پر کنید» و بدون مالک
   برمی‌گشت — دقیقاً همان چیزی که آن حذف برای جلوگیری از آن وجود دارد.
2. کد مرحله (`REGISTRATION`) در فایل Excel کارشناس دیده می‌شد، چون ترجمه نقش فقط
   در کارنامه بود نه در خود دفتر ایراد.

### فاز D — تحویل ✅
- [x] نسخه 29.10.0 (`gsi/factsheet.py` تنها منبع نسخه) + کانال `data-trust-layer-20260926`
- [x] قراردادهای بین‌ماژولی بالا رفت و در `REQUIRED_CONTRACTS` ثبت شد
      (`trust` ۱→۲، `trust.verdict` ۱→۲، `trust.impact` ۱→۲)
- [x] `CHANGELOG.md` + `PACKAGE_INFO.json` (نسخه قبلی به `previous_release` رفت)
- [x] گزارش کار تفصیلی: `gsi-platform/GSI_TRUST_LAYER_REPORT_V29_10_0_FA.md`
- [x] بسته: `GSI_29_10_0.zip` — ۱۰۶۵ فایل
- [x] **تأیید روی استخراج تازه بسته** (نه فقط درخت کاری):
      `gsi.doctor` تمیز · ۱۳۷۸ تست موفق · اجرای کامل خط لوله با ستون‌های `TRUST_*`

**باگ سوم که در همین فاز پیدا شد:** `gsi/MANIFEST.json` هنوز مال ۲۹٫۸٫۲ بود و در
۲۹٫۹٫۰ بازتولید نشده بود؛ `gsi.doctor` روی نصب سالم ۱۷ فایل را «دست‌کاری‌شده»
اعلام می‌کرد. حالا `build_clean_release.py` در هر build بازتولیدش می‌کند و
`tests/test_release_manifest_v29_10.py` قفلش کرده.

**⚠️ تصمیم باز که مال کسب‌وکار است:** نگاشت `BL_DATE`. جزئیات در بخش ۴ گزارش.
تنها اقدامی که به‌تنهایی ۵ پرونده را قابل تصمیم می‌کند.

## ۶) اگر جای مدل قبلی را گرفته‌اید

1. همین فایل + `gsi-platform/docs/DATA_STRATEGY_FA.md` را بخوانید.
2. `git log --oneline -20` را ببینید؛ هر commit یک قدم کامل و تست‌شده است.
3. `HANDOFF_FOR_NEXT_MODEL.md` را بخوانید — دام‌ها و تصمیم‌های گرفته‌شده آنجاست.
4. تست‌ها را اجرا کنید تا مطمئن شوید نقطه شروع سالم است.
5. کار را از اولین آیتم تیک‌نخورده بخش ۵ ادامه دهید.
