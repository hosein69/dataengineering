# راهنمای تحویل به مدل/همکار بعدی

> اگر شما مدل یا همکار بعدی هستید: اول `PROJECT_STATE.md` را بخوانید، بعد این را.
> این فایل «دام‌ها و تصمیم‌های گرفته‌شده» است — چیزهایی که اگر ندانید، دوباره
> همان اشتباه را می‌کنید یا کار درستِ قبلی را خراب می‌کنید.

---

## ۱) در یک نگاه

* **مبنا:** بسته `gsi-platform/` — GSI که در این مخزن دیباگ و بازسازی شده است.
  هر کاری می‌کنید روی همین باشد، نه روی ZIPهای قدیمی.
* **هدف فاز جاری:** داده بد مبنای تصمیم نشود، بدون اینکه چیزی حذف شود؛ و فرهنگ
  داده با نشان‌دادن مالک و «کوچک‌ترین اقدام بعدی» بالا برود.
* **استراتژی:** `gsi-platform/docs/DATA_STRATEGY_FA.md` — **قبل از کد زدن بخوانید.**
* **کد لایه اعتماد:** `gsi-platform/gsi/trust/`
* **تست‌های قفل‌کننده:** `gsi-platform/tests/test_trust_layer_v29_10.py`

قبل از هر commit: `python run_all_tests.py` باید **۰ ناموفق** بدهد.

**قبل از هر انتشار (build بسته):**

```bash
cd gsi-platform
python -m gsi.manifest                      # اثر انگشت فایل‌ها — وگرنه doctor هشدار کاذب می‌دهد
python -m gsi.doctor                        # باید تمیز باشد
python run_all_tests.py                     # ۰ ناموفق
python tools/build_clean_release.py ../GSI_<ver>.zip
```

سپس **بسته را استخراج کنید و همان‌جا `doctor` و تست‌ها را دوباره بزنید.** درخت
کاری سبزبودن کافی نیست؛ باگ مانیفست فقط در همین مرحله پیدا شد.


## ۱.۱) قاعده Continuity و مدل مالکیت — قبل از هر توسعه

این پروژه باید در هر لحظه قابل تحویل به مدل/همکار بعدی باشد. بنابراین **continuity docs
مقدم بر توسعه قابلیت بعدی‌اند**: پیش از شروع یک فاز، وضعیت/تصمیم‌های جدید در
`PROJECT_STATE.md` و این فایل روشن می‌شود؛ در پایان هر commit معنی‌دار نیز این دو سند
باید هنوز نقطه ادامه صحیح را نشان دهند.

اهرم اصلی «فرهنگ داده» مدل مالکیت غنی موجود است و نباید به یک ستون generic تقلیل یابد:

* **مالک تخصصی فیلد** از خانواده `EXPERT_*` اولویت دارد؛ همان نقشی که واقعاً می‌تواند
  آن فیلد را اصلاح کند (مثل settlement / logistics / commercial).
* `ORG_DEPT`، `ORG_UNIT` و `ORG_MANAGER` زمینه سازمانی و مسیر escalation را می‌دهند؛
  UI و export باید بتوانند backlog را در این سطوح نیز slice کنند.
* `CURRENT_OWNER` / owner عمومی فقط fallback است؛ نباید مالک نقش‌محور معتبر را overwrite کند.
* یک نفر ممکن است چند نقش داشته باشد؛ نمایش نقش‌ها باید lossless باشد، نه «آخرین نقش دیده‌شده».
* UX فرهنگ داده باید به‌جای رتبه‌بندی تنبیهی بگوید: **چه کسی، در چه نقش/واحدی، کدام
  کوچک‌ترین اقدام را انجام دهد و آن اقدام چه تصمیم/پرونده‌ای را آزاد می‌کند.**
* هیچ تغییر Phase C مجاز نیست lineage، role-specific ownership یا organizational context
  را برای ساده‌کردن UI حذف کند.

این قرارداد به اندازه قواعد Trust Layer مهم است: **ownership صرفاً metadata نیست؛
مکانیزم عملیاتی تبدیل کیفیت داده به رفتار سازمانی است.**

## ۲) تصمیم‌های معماری که عمدی‌اند (تغییرشان ندهید بدون دلیل قوی)

1. **درجه به جفت (تصمیم، رکورد) می‌چسبد، نه به رکورد.**
   این هسته استراتژی است. یک پرونده می‌تواند برای «کجاست؟» سبز و برای «چقدر
   بدهکاریم؟» قرمز باشد. اگر درجه را به رکورد بچسبانید، مجبور می‌شوید بین
   «حذف شاهد سالم» و «رسیدن عدد غلط به تصمیم» یکی را انتخاب کنید.

2. **جمع مبلغ (ADDITIVE) سخت‌گیرتر از توزیعی (DISTRIBUTIONAL) است.**
   جمعی که یک جزء نامعلوم دارد غلط است؛ رتبه‌بندی با ۹۴٪ پوشش معمولاً همان
   رتبه‌بندی درست است. این تفکیک در `fitness.py` است و تست دارد.

3. **شمارش در دانه موجودیت است، نه ردیف.**
   مارت نهایی دانه بارنامه×متریال دارد ولی مانده تعهد در دانه ثبت سفارش است.
   شمارش ردیفی یک سلول خالی را ۴۰ بار گزارش می‌کند. `profiling.py` تضمین می‌کند
   هر (موجودیت، فیلد، کد) حداکثر یک ایراد بدهد.

4. **`FIELD_NEVER_POPULATED` — مهم‌ترین محافظ اعتبار.**
   فیلدی که برای *همه* پرونده‌ها خالی است، تقریباً هیچ‌وقت «هزار نفر یک سلول را
   فراموش کرده‌اند» نیست؛ فیلدی است که خط لوله نمی‌خواندش. اگر به‌صورت
   data-entry ارجاع شود، کارشناس می‌رود می‌بیند سلول در سورس پر است و **از آن
   لحظه به کل فهرست بی‌اعتماد می‌شود.** یک ایراد در سطح قرارداد سورس ثبت
   می‌شود، نه یکی به‌ازای هر پرونده.

5. **`is_empty_val` هسته را در لایه اعتماد استفاده نکنید.**
   آن تابع معنایی است و «نامشخص» و «-» را خالی می‌داند — برای منطق کسب‌وکار
   درست است و برای اینجا غلط. `profiling._blank()` فقط خالیِ فیزیکی را می‌سنجد
   تا بتوانیم بگوییم «۱۸۴۰ سلول با مقدار «نامشخص» پر شده» که عملیاتی‌ترین
   تشخیص ممکن است.

6. **معیار عملکرد = نرخ بهبود، نه مقدار مطلق.**
   کارنامه مالک بر اساس «پرونده قابل آزادسازی» مرتب می‌شود، نه «بیشترین ایراد».
   رتبه‌بندی تنبیهی، پنهان‌کاری تولید می‌کند نه داده تمیز.

7. **قرارداد سورس/authority بدون تأیید مالک کسب‌وکار تغییر نمی‌کند.**
   نمونه زنده: `BL_DATE` از `BL_BL_DATE` مشتق می‌شود که **هیچ adapterی تولیدش
   نمی‌کند** (پس همیشه خالی است و رویداد حمل، سررسید برات و کنترل
   «ترخیص قبل از حمل» را بی‌صدا از کار انداخته). این **گزارش** شد و عمداً
   خودسرانه remap **نشد**.

## ۳) دام‌هایی که قبلاً در آن‌ها افتاده‌ایم

| دام | چه شد | درس |
|---|---|---|
| نام ماژول هم‌نام کتابخانه استاندارد | `gsi/trust/profile.py` با `profile` پایتون تصادم کرد؛ تست `test_import_hygiene` گرفت | اسم ماژول را با stdlib چک کنید (`numbers`، `profile`، `calendar`…) |
| شمارش (تصمیم × پرونده) به‌جای پرونده | «۱ سلول ← ۲ پرونده آزاد می‌شود» تولید شد که عدد بی‌معناست | هر وعده به مالک باید حسابی صادق باشد؛ با set بشمارید نه شمارنده |
| بلعیده‌شدن placeholder | `is_empty_val` «نامشخص» را خالی می‌دید، پس `VALUE_PLACEHOLDER` هرگز رخ نمی‌داد | تست بنویسید که *وجود* یک کد را ثابت کند، نه فقط نبود خطا |
| f-string مخصوص ۳٫۱۲ | داشبورد روی ۳٫۱۱ (حداقل اعلام‌شده) اصلاً بالا نمی‌آمد | تست کامپایل همه فایل‌ها وجود دارد؛ حذفش نکنید |
| حذف از دفتر، بدون اطلاع لایه‌های بالاتر | `drop_field` ایرادهای per-case فیلد نگاشت‌نشده را برمی‌داشت، ولی `rank_opportunities` فهرست را از روی **وضعیت فیلدها** می‌سازد که هنوز MISSING است — پس همان فیلد دوباره به شکل «۵ سلول را پر کنید» و بدون مالک برمی‌گشت | هر حذف/استثنا باید در خود دفتر **ثبت** شود (`_mapping_gaps`) تا مصرف‌کننده‌های پایین‌دست هم ببینندش؛ حذف بی‌صدا یعنی همان اشتباه یک لایه بالاتر |
| ترجمه فقط در لایه نمایش | نقش در کارنامه «ثبت سفارش» بود ولی در Excel کارشناس `REGISTRATION` | ترجمه را کنار داده بگذارید (`verdict.role_fa`)، نه در هر مصرف‌کننده جداگانه |
| مانیفست کهنه، هشدار کاذب | `gsi/MANIFEST.json` از ۲۹٫۸٫۲ مانده بود؛ `gsi.doctor` روی نصب کاملاً سالم ۱۷ فایل را «دست‌کاری‌شده» اعلام می‌کرد | هشدار کاذب، ابزار تشخیص را خاموش می‌کند. **هر انتشار:** `python -m gsi.manifest` — حالا `build_clean_release.py` خودکار انجامش می‌دهد و تست دارد |
| کش ماژول در Streamlit | تغییر `app/trust_view.py` با rerun اعمال نمی‌شد چون `import` داخل تابع است و پایتون ماژول را کش می‌کند | برای دیدن تغییر این فایل، **سرور را ری‌استارت کنید**؛ rerun کافی نیست |

## ۴) کجا چه چیزی است

```
gsi-platform/
  docs/DATA_STRATEGY_FA.md      استراتژی (اول این)
  docs/ARCHITECTURE_FA.md       معماری کل محصول
  gsi/trust/
    codes.py        تاکسونومی ایراد — افزودن کد جدید فقط اینجا
    verdict.py      نمونه ایراد + دفتر (append-only)
    profiling.py    سنجش نقطه صفر (نام فایل عمداً profile نیست)
    fitness.py      دروازه تصمیم و درجه‌بندی
    impact.py       کوچک‌ترین اقدام بعدی + کارنامه مالک
    contracts.py    قراردادهای تصمیم GSI — افزودن تصمیم جدید فقط اینجا
    trend.py        روند بهبود بین اجراها
  app/trust_view.py             صفحه «اعتماد داده و کیفیت» (۵ تب)
  gsi/stages/s95_trust.py       اتصال به خط لوله
  gsi/warehouse/bridge.py       تابع trust_snapshot — ماندگاری روند
  tests/test_trust_layer_v29_10.py
```

**افزودن یک تصمیم جدید** = یک `DecisionContract` در `contracts.py`. هیچ فایل
دیگری لازم نیست عوض شود؛ درجه، فیلدهای مسدودکننده، فهرست کار مالک و روند،
همه خودکار دنبال می‌کنند.

**افزودن یک سطح سازمانی جدید** (مثلاً معاونت) = یک ورودی در `impact.ORG_LEVELS`
به‌صورت `"نام": ("عنوان ستون", "صفتِ Owner")` و یک ورودی در `_ORG_VIEWS` در
`app/trust_view.py`. مقدار آن صفت باید روی `Owner` موجود باشد (`verdict.py`).

## ۵) کاری که نیمه‌تمام است

بخش ۵ فایل `PROJECT_STATE.md` فهرست تیک‌خورده/نخورده را دارد. از اولین آیتم
تیک‌نخورده ادامه دهید.

## ۶) اگر توکن مدل قبلی تمام شده و چیزی خراب مانده

1. `git log --oneline -20` — هر commit یک قدم کامل و سبز است.
2. `cd gsi-platform && python run_all_tests.py` — اگر سبز بود، نقطه شروع سالم است.
3. اگر قرمز بود: `git diff HEAD~1` را ببینید و **برگردید به آخرین commit سبز**
   (`git revert` یا `git checkout <sha> -- <path>`)، نه اینکه روی کد خراب بسازید.
4. کار ناتمام هیچ‌وقت commit نشده است؛ پس HEAD همیشه قابل اجراست.
