"""Single source of truth for the engine version.

The version is stamped into every SignalCard so that a stored signal can always
be replayed against the exact code that produced it.
"""

VERSION = "5.0.0"
CONTRACT_VERSION = "2"
