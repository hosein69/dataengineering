# پلتفرم هوش فرآیندی (Process Mining UI Kit) — وابستگی‌های اجرا
# پایتون ۳٫۹ یا بالاتر

streamlit>=1.35
plotly>=5.20
openpyxl>=3.1

# اختیاری — اگر روی محیط اجرا نصب باشد theme.py/persian.py خودکار از آن
# استفاده می‌کنند؛ نبودش هیچ‌چیزی را نمی‌شکند (مبدل شمسی داخلی جایگزین است).
# jdatetime

# درگ‌اند‌درپ چیدمان (بررسی UX v2): دیگر به یک بستهٔ بیرونی نیاز ندارد.
# pm_ui/layout/dragdrop.py همان قابلیت را با declare_component + یک
# index.html خام (پروتکل postMessage خودِ Streamlit) پیاده‌سازی می‌کند —
# بدون نصب از اینترنت، سازگار با محیط کاملاً آفلاین. (نسخهٔ قبلی این خط
# بستهٔ streamlit-sortables را پیشنهاد می‌داد؛ آن نیاز دیگر برطرف شده است.)
