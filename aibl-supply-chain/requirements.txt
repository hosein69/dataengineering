# AIBL — وابستگی‌های اجرا
# پایتون ۳٫۱۰ یا بالاتر (این بسته روی ۳٫۱۳ نیز اعتبارسنجی شده)

# ── هسته خط لوله ──
pandas>=2.0
numpy>=1.24
openpyxl>=3.1
PyYAML>=6.0          # کتابخانه قوانین aibl/rules/*.yaml بدون این بالا نمی‌آید

# ── داشبورد و Studio ──
streamlit>=1.35
plotly>=5.20
streamlit-sortables==0.3.1

# ── بسته ایمیل مدیریتی (python -m aibl email) ──
matplotlib>=3.7      # نمودارهای PNG ایمیل؛ نبودش یعنی ModuleNotFoundError
arabic-reshaper>=3.0   # شکل‌دهی حروف فارسی در PNG نمودار ایمیل
python-bidi>=0.6       # راست‌به‌چپ صحیح عنوان/برچسب نمودارهای matplotlib

# ── اختیاری ──
# jdatetime         # مبدل شمسی داخلی هست؛ این فقط جایگزین است

# اختیاری: ارسال مستقیم از Classic Outlook روی Windows
# pywin32>=306
